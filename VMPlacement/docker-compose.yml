services:
  vm-placement:
    build: .
    container_name: vm-placement
    ports:
      - "8080:8080"
    restart: unless-stopped
    networks:
      - pucp_cloud_net
 
networks:
  pucp_cloud_net:
    external: true